import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.ziterz.app',
  appName: 'vue3-native-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
